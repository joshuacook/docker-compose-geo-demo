version: '3'
services:
    jupyter:
        build: docker/jupyter
        ports:
            - 8898:8888
        volumes:
            - .:/home/jovyan
    postgis:
        build: docker/postgis
        volumes:
            - postgres_data:/var/lib/postgresql/data
volumes:
    postgres_data:

